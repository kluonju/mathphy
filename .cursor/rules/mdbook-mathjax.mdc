---
description: mdBook MathJax escaping for concise-math-physics Markdown
globs: concise-math-physics/**/*.md
alwaysApply: false
---

# mdBook 数学公式写法（必须遵守）

写或改 `concise-math-physics/**/*.md` 时严格按下列约定。完整说明见 `concise-math-physics/MATHJAX.md` 与 `concise-math-physics/README.md`「公式写法」一节。

本书用 mdBook + MathJax（`book.toml`：`mathjax-support = true`）。

## 定界符

- 行内：`\\( ... \\)`（源文件里是**两个**反斜杠再加括号）
- 独立：单独一行的 `\\[` 与 `\\]`
- 公式内的 TeX 命令用**单个**反斜杠：`\mathbf`、`\frac`、`\partial`

## 下标：必须转义 `_`

Markdown 会把 `_x` 吃成 `<em>`。源文件中凡在数学定界符内的下划线都写成 `\_`。

```markdown
正确：\\(A\_x\\)、\\(\mathbf{i}\_x\\)、\\(\partial\_x\\)、\\(\lim\_{n\to\infty}\\)
错误：\\(A_x\\)、\\(\mathbf{i}_x\\)
```

## 细空格：必须写 `\\,`

Markdown 会把 `\,` 当成转义逗号，变成普通 `,`，公式显示为 `f(x),mathrm{d}x`。

```markdown
正确：\\(\int f(x)\\,\mathrm{d}x\\)
错误：\\(\int f(x)\,\mathrm{d}x\\)
```

## 星号上标：不要写 `^*`

`*` 会被当成 Markdown 强调。用 `\ast`：

```markdown
正确：\\(x^{\ast}\\)
错误：\\(x^*\\)
```

## 其它注意

- 独立公式行首不要用 `+` / `-` / `*`（会被当成列表）；用 `aligned` 或把符号放在行中
- 图注（`![...](...)`）里不要写 `\\(...\\)`：alt 不经 MathJax，用纯文字（如 `θ`、`A·dS`）
- 括号要配对：`\mathbf{i}_x` 写作 `\\(\mathbf{i}\_x\\)`

## 批量修复

下标类问题可运行：

```bash
python3 concise-math-physics/scripts/fix_math_underscores.py
```

（该脚本不自动修复错误的 `\,` 或行首 `+`。）
